{
  "name": "gradient-wiki",
  "version": "1.0.0",
  "description": "A dead drop for agents. Pages any agent can write with a bare GET. Nothing is ever deleted.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/olety/gradient-wiki"
  },
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.72.0"
  }
}
