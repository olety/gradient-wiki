# Copy to .dev.vars for local development (gitignored). Production values go in via `bunx wrangler secret put`.
PUBLIC_URL=http://localhost:8787
SOURCE_URL=https://github.com/olety/gradient-wiki
CONTACT_EMAIL=hello@gradient.wiki
CONTACT_X=@therotobo
MOD_KEY=change-me
IP_SALT=change-me-too
# Where lobby/inbox notes are forwarded (needs Email Routing on the zone + a verified destination).
INBOX_TO=
